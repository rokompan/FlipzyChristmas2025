{% schema %}
{
  "name": "Flipzy Feature Video",
  "class": "flipzy-section",
  "settings": [
    {
      "type": "header",
      "content": "Color & Layout"
    },
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Color Scheme",
      "options": [
        { "value": "scheme-white", "label": "White" },
        { "value": "scheme-cream", "label": "Cream" },
        { "value": "scheme-sage", "label": "Sage" },
        { "value": "scheme-dark", "label": "Dark" }
      ],
      "default": "scheme-white"
    },
    {
      "type": "select",
      "id": "desktop_layout",
      "label": "Desktop Layout",
      "default": "media_right",
      "options": [
        { "value": "media_left", "label": "Video Left" },
        { "value": "media_right", "label": "Video Right" }
      ]
    },
    {
      "type": "select",
      "id": "mobile_layout",
      "label": "Mobile Layout",
      "default": "text_first",
      "options": [
        { "value": "text_first", "label": "Text First" },
        { "value": "media_first", "label": "Video First" }
      ]
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Title",
      "default": "What is Flipzy?"
    },
    {
      "type": "header",
      "content": "Media"
    },
    {
      "type": "text",
      "id": "video_url",
      "label": "Video URL (MP4)",
      "info": "Upload to Settings > Files, paste URL here."
    },
    {
      "type": "image_picker",
      "id": "fallback_image",
      "label": "Fallback Image"
    }
  ],
  "blocks": [
    {
      "type": "feature_point",
      "name": "Feature Point",
      "settings": [
        {
          "type": "select",
          "id": "icon_type",
          "label": "Icon",
          "options": [
            { "value": "bulb", "label": "Lightbulb (Idea)" },
            { "value": "puzzle", "label": "Puzzle Piece" },
            { "value": "cycle", "label": "Cycle / Refresh" },
            { "value": "wrench", "label": "Wrench / Tools" },
            { "value": "check", "label": "Checkmark" },
            { "value": "star", "label": "Star" }
          ],
          "default": "check"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>Feature text here</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Flipzy Feature Video",
      "blocks": [
        { 
          "type": "feature_point", 
          "settings": { 
            "icon_type": "bulb",
            "text": "<p>A wooden board that helps kids follow daily routines.</p>" 
          }
        },
        { 
          "type": "feature_point", 
          "settings": { 
            "icon_type": "puzzle",
            "text": "<p>7 customizable wooden pieces</p>" 
          }
        },
        { 
          "type": "feature_point", 
          "settings": { 
            "icon_type": "cycle",
            "text": "<p>Simple flip action to mark tasks as complete</p>" 
          }
        },
        { 
          "type": "feature_point", 
          "settings": { 
            "icon_type": "wrench",
            "text": "<p>Easy to mount anywhere</p>" 
          }
        }
      ]
    }
  ]
}
{% endschema %}

<div class="flipzy-feature-section {{ section.settings.color_scheme }} layout-desktop-{% if section.settings.desktop_layout == 'media_left' %}left{% else %}right{% endif %} layout-mobile-{{ section.settings.mobile_layout }}">
  <div class="flipzy-feature-inner">
    
    <div class="flipzy-feature-media">
      <div class="flipzy-feature-media-inner">
        {% if section.settings.fallback_image != blank %}
          <img
            class="flipzy-feature-image"
            src="{{ section.settings.fallback_image | image_url: width: 1200 }}"
            alt="{{ section.settings.heading | escape }}"
            loading="lazy"
          >
        {% endif %}

        {% if section.settings.video_url != blank %}
        <video
          class="flipzy-feature-video"
          autoplay muted loop playsinline
          preload="none"
          {% if section.settings.fallback_image != blank %}
            poster="{{ section.settings.fallback_image | image_url: width: 1200 }}"
          {% endif %}
          aria-hidden="true"
        ></video>
        {% endif %}
      </div>
    </div>

    <div class="flipzy-feature-content">
      {% if section.settings.heading != blank %}
        <h2 class="flipzy-feature-heading">{{ section.settings.heading }}</h2>
      {% endif %}

      <div class="flipzy-feature-points">
        {% for block in section.blocks %}
          <div class="flipzy-feature-point" {{ block.shopify_attributes }}>
            <div class="flipzy-feature-icon-box">
              {% case block.settings.icon_type %}
                {% when 'bulb' %}
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2a7 7 0 0 0-7 7c0 2.38 1.19 4.47 3 5.74V17a2 2 0 0 0 2 2h4a2 2 0 0 0 2-2v-2.26C17.81 13.47 19 11.38 19 9a7 7 0 0 0-7-7z"></path><path d="M9 21h6"></path></svg>
                {% when 'puzzle' %}
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19.439 15.424a1 1 0 0 0-1.025.99c0 .605.342 1.586.342 1.586.683 1.93-1.082 3.69-3.007 3s-1.442-2.502-3.483-2.502-2.31 1.767-2.31 1.767c-.66 2.05-3.05 1.536-3.71.696-.66-.84-.342-2.486-.342-2.486a1 1 0 0 0-1.025-.99 1 1 0 0 0-1.025.99c0 .605.342 2.652.342 2.652 1.346 3.86 6.22 4.545 7.562 2.652 1.342 1.893 6.216 1.208 7.562-2.652 0 0 .342-2.047.342-2.652a1 1 0 0 0-1.025-.99z"></path></svg>
                {% when 'cycle' %}
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21.5 2v6h-6"></path><path d="M21.34 5.5A10 10 0 1 0 22 12"></path></svg>
                {% when 'wrench' %}
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path></svg>
                {% when 'check' %}
                   <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
                {% when 'star' %}
                   <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon></svg>
              {% endcase %}
            </div>
            <div class="flipzy-feature-point-text">{{ block.settings.text }}</div>
          </div>
        {% endfor %}
      </div>
    </div>

  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const container = document.currentScript.previousElementSibling;
    const video = container.querySelector('.flipzy-feature-video');
    const image = container.querySelector('.flipzy-feature-image');
    
    if (!video) return;
    
    const videoSrc = "{{ section.settings.video_url }}";
    if (!videoSrc) return;

    function isSlowConnection() {
      const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
      if (!connection) return false;
      return connection.saveData || connection.effectiveType === 'slow-2g' || connection.effectiveType === '2g';
    }

    const loadVideo = function () {
      if (video.dataset.loaded === 'true') return;
      video.src = videoSrc;
      video.dataset.loaded = 'true';
    };

    if (!isSlowConnection()) {
      loadVideo();
    }

    video.addEventListener('loadeddata', function () {
      video.style.display = 'block';
      if (image) image.classList.add('is-hidden');
    });
  });
</script>